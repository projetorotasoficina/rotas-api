version: "3.8"

services:
  rotas-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rotas-api
    restart: always
    env_file:
      - .env
    ports:
      - "8080:8080"
