app:
  client:
    origins: ${APP_ALLOWED_ORIGINS:http://localhost:3000}
  security:
    jwt:
      secret-key: ${JWT_SECRET_KEY}
      expiration-time: ${JWT_EXPIRATION_TIME:7200000} # 2h
  swagger:
    enabled: ${SWAGGER_ENABLED:false}

spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: false
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  config:
    import: optional:file:.env[.properties]

# Removi a configuração de teste duplicada daqui
# Ela deve ficar apenas no application-test.yml

